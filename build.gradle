group "tech.arauk"
version "1.0-SNAPSHOT"

subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    sourceCompatibility = 1.7

    tasks.withType(JavaCompile) {
        options.encoding = "UTF-8"
    }
}
